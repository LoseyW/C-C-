// save the following file as hello.c
// compile as $gcc hello.c -o hello
// to run type ./ hello or /home/user/ccode/hello

#include <stdio.h>

int main(){
        int i;
        for(i = 0; i < 10; i++){
       printf(" %d Hello World \n",i);
    }
       return 0;
}
